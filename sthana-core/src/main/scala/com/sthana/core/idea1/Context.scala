package com.distribute.core

/**
 * A context is a mutable-snapshot. Mutations to a snapshot occur within a transaction context. If a
 * transaction is successful, the context applies the result to the underlying snapshot.
 */
class Context {

}
